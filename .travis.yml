# Config file for automatic testing at travis-ci.org

sudo: false
language: python
python: 3.8
env:
    - TOX_ENV=py35
    - TOX_ENV=py36
    - TOX_ENV=py37
    - TOX_ENV=py38

script: tox -e $TOX_ENV

install:
    - pip install tox
